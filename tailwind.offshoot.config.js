// Tailwind config for the isolated Eldritch RPG offshoot page.
// Keeps purge scope tight so main app CSS stays lean.
module.exports = {
  content: ["./offshoot/erp_pcs.html"],
  theme: { extend: {} },
  plugins: [],
};
